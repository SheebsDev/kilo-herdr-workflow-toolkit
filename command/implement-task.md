---
description: "Implement a Task Card and run the project's parallel engineering review workflow"
agent: code
---

# Implement Task

Load and follow the canonical `implement-task` Agent Skill from
`skills/implement-task/SKILL.md`, using the requested Task Card or task context
as its input. The skill is the only authored source for the coordinator
procedure and applies to Kilo, Claude Code, and Codex coordinators.
